export interface TokenInfo {
    user_id: string;
    role: string;
    email: string;
    isEmailVerified: boolean;
}
export interface Navlink {
    name: string;
    routerLink: string;
    icon: string;
}